"""ホーム — ラボの一覧と、この先の進め方。"""

from __future__ import annotations

import streamlit as st

from app.components.cards import LabCard, lab_grid
from app.components.layout import page_header, panel

page_header(
    number=0,
    title="ML Lab — 触って身につける機械学習",
    lede=(
        "スライダを動かすと、決定境界・損失曲面・クラスタが その場で 変わります。"
        "数式を追う前に「何が起きているか」を目で捉えることを狙ったラボ集です。"
        "左のメニューから、上から順に進めるのがおすすめです。"
    ),
    eyebrow="ML LAB / PHASE 1",
)

panel("基礎ラボ", "ドメインに依存しない、機械学習の土台となる 7 テーマ")

lab_grid(
    [
        LabCard(
            1, "決定境界",
            "同じデータでも、モデルが違えば境界の形はまるで違う。"
            "<b>ロジスティック回帰・kNN・SVM・決定木・ランダムフォレスト</b>を"
            "切り替えて、引かれる線を見比べます。",
        ),
        LabCard(
            2, "過学習と正則化",
            "多項式の次数を上げていくと、訓練データには完璧に当たるのに"
            "未知データで崩れる。<b>訓練誤差と検証誤差が分かれる瞬間</b>を"
            "1 枚のグラフで捉えます。",
        ),
        LabCard(
            3, "勾配降下法",
            "モデルが「学習する」とは、損失の谷を下ること。"
            "<b>学習率を上げすぎると発散し、下げすぎると進まない</b>様子を"
            "等高線の上の軌跡で確かめます。",
        ),
        LabCard(
            4, "クラスタリング",
            "正解ラベルなしでデータをまとめる。k-means を"
            "<b>1 ステップずつ手で進めて</b>重心が動く様子を見たあと、"
            "DBSCAN・階層クラスタリング・GMM と比べます。",
        ),
        LabCard(
            5, "次元削減",
            "64 次元の手書き数字を 2 次元の地図に落とす。"
            "<b>PCA と t-SNE で地図の形がどう変わるか</b>、"
            "点をクリックして元画像を確かめながら掴みます。",
        ),
        LabCard(
            6, "評価指標",
            "「精度 98%」はなぜ嘘になりうるのか。"
            "<b>閾値と不均衡のスライダ</b>を動かすと、混同行列・ROC・PR 曲線が"
            "連動して動き、指標の意味が腑に落ちます。",
        ),
        LabCard(
            7, "アンサンブル",
            "弱い決定木を束ねると、なぜ強くなるのか。"
            "<b>木 1 本 → バギング → ランダムフォレスト → 勾配ブースティング</b>を"
            "同じデータで並べて比較します。",
        ),
    ]
)

panel("使い方", "3 つだけ覚えれば十分です")

st.markdown(
    """
1. **左のサイドバーを動かす** — データの形・ノイズ・ハイパーパラメータはすべてここにあります。
   1 つだけ動かして、図がどう変わるかを見るのがコツです。
2. **図の下の解説を読む** — 各ラボの下部に、いま見ているものの意味と
   「どこを見ればいいか」を書いてあります。
3. **乱数シードを変えてみる** — 同じ設定でもデータが変われば結果は揺れます。
   その揺れの大きさ自体が、モデルの安定性の情報になります。
"""
)

panel("この先の予定", "Phase 2 以降で足していくもの")

st.markdown(
    """
基礎ラボで土台ができたら、実データを扱う層を重ねていきます。

- **データ収集〜蓄積** — e-Stat（政府統計）・気象庁・株価・RSS を取得し、
  Parquet に落として DuckDB で SQL 解析できるようにする
- **ドメイン別ラボ** — テーブル / 時系列 / テキスト / 画像・信号
- **実験管理** — MLflow で「何を試して、どうだったか」を残す
- **重い計算** — 深層学習が必要になった段階で Google Colab の無料 GPU に逃がす

すべてローカル完結・無料で回せる構成のままです。
"""
)

st.caption(
    "環境: Python 3.11 / scikit-learn / Plotly / Streamlit　—　"
    "計算ロジックは `mllab/`、画面は `app/views/`、解説は `content/` にあります。"
)
